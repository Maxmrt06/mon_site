{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
{# <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style> #}

    <h1>Bienvenue sur la page d'accueil</h1>

    <div class="row">
       {% for article in articles %}

        <div class="card border-primary md-4" style="max-width: 20rem;">
            
            <img class="card-img-top" src="{{ article.image }}" alt="{{ article.title }}">
            <div class="card-body">
                <h4 class="card-title">{{ article.title }}</h4>
                <a href="{{ path('show',{'id':article.id}) }}" class="btn btn-primary">En savoir plus</a>
                <p class="date">Ecrit le {{ article.createdAt | date("d/m/Y") }} à {{ article.createdAt | date("H:i") }}</p>
            </div>
        </div>

       {% endfor %}
    </div>

    

   
{% endblock %}
