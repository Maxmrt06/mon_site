{% extends 'base.html.twig' %}

{% block title %}Articlessss n° {{article.id}}{% endblock %}

{% block body %}
{# <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style> #}

<div class="container">
    <h2 class="title text-center p-1">{{ article.title }}</h2>
    <div class="img-fluid">
        <img width="60%" src="{{ article.image }}" alt="{{ article.title }}">
    </div>
    <p class="content text-justify">
        {{ article.content }}
    </p>
    <p class="date">Ecrit le {{ article.createdAt | date("d/m/Y") }} à {{ article.createdAt | date("H:i") }}</p>
</div>   


{% endblock %}


